normalise_data = function(physeq = without_mock_physeq,
                          norm_method = NULL,
                          copy_correction = TRUE) {

  if (copy_correction == TRUE) {
    print()

    # misschien niks laten zien en gewoon door gaat tot je bij de plots bent

  } else if (copy_correction == FALSE) {
    print
  }
}


